<!--
rules.md: the rules and lessons I carry, not the live handoff (that's state.md).
Two parts:
  1. Global — rules and lessons that hold for EVERY project: the principles I work by, plus
     lessons promoted from projects once reworked. This is what makes each new project start
     smarter than the last.
  2. From this project — rules/lessons born here that look generalizable but aren't promoted
     yet. /end adds to this; once reworked they graduate up into Global.
Project-only rules that will never generalize don't live here — they stay in state.md's
"Rules & lessons". Keep this file short and high-signal: prune, don't append blindly.
-->

# rules.md

## Global

How I want you to work — the rules every project inherits.

- **Context is scarce.** Keep every file short and high-signal. Say things once. Prune hard
- **Record the why, not the what.** The what is in the code and git. The reasoning behind a decision is what gets lost and causes drift. Capture it.
- **Build in vertical slices.** Each change cuts through the whole stack and ships something that works. No half-finished layers.
- **Never grade your own work.** Check against something external: tests, a build, a screenshot, a fresh-context review. "Done" you decided alone doesn't count.
- **Split stable from mutable.** The durable rules (here) and the live scratchpad (state.md) are separate files on purpose — don't treat everything as equally editable.

## From this project

Rules and lessons born here that could help other projects — candidates to promote into Global once reworked.
