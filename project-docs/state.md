<!--
state.md — a project's live handoff. Created and updated by /end at each session's close; read by /start.
Keep it short. Rewrite Now / Short term / Long term each session — never append there, they're not a log.
"Rules & lessons" is different: it's stable — add and prune it deliberately, don't churn it.
Record the why, not the plain what: the what is already in the code and git.
-->

# state.md - <project name>

_Updated: <YYYY-MM-DD>_

## Now
What exists and works as of the last session's end. A snapshot, not a play-by-play.

## Short term
A checklist of what to do next, over the coming sessions — concrete, decided actions.
Any decision that comes up and gets made lands here (or in Long term).

## Long term
The roadmap toward the goal (the goal itself lives in `<project-name>.md`).
Direction and milestones — and, if useful, what the project will not become.

## Rules & lessons
Rules and lessons specific to this project that you must remember across sessions:
gotchas, conventions, decisions that still bind. Stable — add and prune deliberately.
A lesson that would help other projects too? Promote it to `rules.md` instead.
