<!--
The project's context file: what this project is and why it exists. Copy it into the project root next to CLAUDE.md, rules.md and state.md, and rename it to the project folder's name, lowercased.

It is the stable layer. /new-project writes it from the opening interview, /adopt rebuilds it from what a project already holds. It changes only when the project itself changes — never as a session log. What is live and moving lives in state.md.

Every section must say something real. Drop the ones that would be padding: an empty section is worse than a missing one. Write it in the user's own first-person voice, short and high-signal.

What each section holds:
- What it is — the project in a few sentences: what it does, and where it lives (repo, deployment) if that exists yet. Add who it's for when the users are real and named.
- Why it exists — the problem it solves and the reasons it is being built. The honest ones, including the selfish ones.
- Where it's heading — the goal and the direction toward it. Also what the project will deliberately not become, wherever that is a live temptation.
- Guiding principles — the decisions that bind every later choice. Each one is a rule a future session can be held to, not an aspiration.
- Shared vocabulary — the project's own terms, defined once each. The section that saves the most confusion later: write it even when it feels premature.

These five are the floor, not the ceiling. Add a section when the project genuinely needs one — "Who it's for", "The problem", "Initial focus" all earn their place in existing projects.

The H1 is the file's own name: the project folder, lowercased.

This file explains the project; it does not index the project's files. CLAUDE.md does the routing inside a project. Rewritten by hand when the project itself changes: no skill maintains it after it's written.
-->

# <name>

## What it is

## Why it exists

## Where it's heading

## Guiding principles

## Shared vocabulary
